name: Legacy deploy workflow (manual only)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "This legacy workflow is disabled."
